# Beauty Backend API

API para sistema de agendamentos da Brotas Beauty.

## 🚀 Instalação

```bash
# Clone o repositório
git clone https://github.com/joaodddev/beauty-backend.git
cd beauty-backend

# Instale as dependências
npm install

# Inicie o servidor
npm run dev

# Endpoints

Serviços
GET /api/services - Listar todos os serviços

Agendamentos
GET /api/appointments - Listar agendamentos

POST /api/appointments - Criar agendamento

GET /api/appointments/today - Agendamentos de hoje

GET /api/available-times - Horários disponíveis

Admin
GET /api/admin/dashboard - Dashboard administrativo

POST /api/admin/login - Login admin

Tecnologias
Node.js
Express.js
CORS

# 
## 🧪 **Como Testar:**

### **Passo 1: Configurar localmente**
```bash
# No terminal, na pasta do projeto:
npm install
npm run dev
